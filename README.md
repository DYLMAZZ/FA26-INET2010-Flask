# Setup Instructions
## Pre-reqs
This project requires python 3.12.x

## Steps
1. In the project root directory, setup python virtual enviroment
`python3 -m venv venv`

2. Install Flask Library
`pip install flask`